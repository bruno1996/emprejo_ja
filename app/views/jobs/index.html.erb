Emprego Já
<% @jobs.each do |job| %>
  <h1>  <%= link_to job.title, job_path(job) %> </h1>
  <%= job.company.name %>
  <%= job.location %>

  <% if job.featured == true %>
     <strong>Destaque</strong>
  <% end %>
<%end%>
